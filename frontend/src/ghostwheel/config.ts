export const ENV = process.env.NODE_ENV;
export const isDev = ENV === 'development';
export const local = '//localhost:3000';
export const prod = '//localhost:3000'; // lol
export const gardyn = local;
